# Jira Configuration for restack-gen project# Jira Configuration for restack-gen project# Jira Configuration for restack-gen project# Jira Configuration for restack-gen project

JIRA_EMAIL=YOUR_EMAIL@DOMAIN.COM

JIRA_SITE=https://hodgedomain.atlassian.netJIRA_EMAIL=YOUR_EMAIL@DOMAIN.COM

JIRA_API_TOKEN=YOUR_JIRA_API_TOKEN_HERE

JIRA_SITE=https://hodgedomain.atlassian.netJIRA_EMAIL=YOUR_EMAIL@DOMAIN.COMJIRA_EMAIL=YOUR_EMAIL@DOMAIN.COM

# Base64 encoded auth for GitHub Actions

# This is automatically generated from your email and API token aboveJIRA_API_TOKEN=YOUR_JIRA_API_TOKEN_HERE

# Format: base64(email:api_token)

# Generate with: echo -n "your_email:your_api_token" | base64JIRA_SITE=https://hodgedomain.atlassian.netJIRA_SITE=https://hodgedomain.atlassian.net

JIRA_AUTH_TOKEN_BASE64=YOUR_BASE64_ENCODED_TOKEN_HERE
# Base64 encoded auth for GitHub Actions (email:token)

# You need to encode: your_email@domain.com:YOUR_JIRA_API_TOKEN_HEREJIRA_API_TOKEN=YOUR_JIRA_API_TOKEN_HEREJIRA_API_TOKEN=YOUR_JIRA_API_TOKEN_HERE

# Use this command to generate: echo -n "your_email@domain.com:YOUR_JIRA_API_TOKEN_HERE" | base64

JIRA_AUTH_TOKEN_BASE64=YOUR_BASE64_ENCODED_TOKEN_HERE

# Base64 encoded auth for GitHub Actions (email:token)# Base64 encoded auth for GitHub Actions (email:token)

# You need to encode: your_email@domain.com:YOUR_JIRA_API_TOKEN_HERE# You need to encode: your_email@domain.com:YOUR_JIRA_API_TOKEN_HERE

# Use this command to generate: echo -n "your_email@domain.com:YOUR_JIRA_API_TOKEN_HERE" | base64# Use this command to generate: echo -n "your_email@domain.com:YOUR_JIRA_API_TOKEN_HERE" | base64

JIRA_AUTH_TOKEN_BASE64=YOUR_BASE64_ENCODED_TOKEN_HEREJIRA_AUTH_TOKEN_BASE64=YOUR_BASE64_ENCODED_TOKEN_HERE