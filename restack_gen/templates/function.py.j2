# @generated by restack_gen v{{ generator_version }} ({{ timestamp }})
# command: {{ command }}
"""{{ name }} function implementation."""

from restack_ai.function import function, log

from {{ project_name }}.common.retries import DEFAULT_RETRY
from {{ project_name }}.common.settings import settings


@function.defn(name="{{ function_name }}")
async def {{ function_name }}({{ params_signature }}) -> {{ return_type }}:
    """
    {{ description }}
    {% if params %}

    Args:
    {%- for param in params %}
        {{ param.name }}: {{ param.description }}
    {%- endfor %}
    {% endif %}

    Returns:
        {{ return_description }}
    """
    log.info(
        "{{ function_name }} called",
        extra={
            {%- for param in params %}
            "{{ param.name }}": {{ param.name }},
            {%- endfor %}
        },
    )

    # TODO: Implement function logic
    {% if return_type == "None" -%}
    pass
    {%- else -%}
    raise NotImplementedError("{{ function_name }} not yet implemented")
    {%- endif %}
