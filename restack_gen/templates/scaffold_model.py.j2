
# @generated by restack-gen
from {{ project_name }}.common.compat import BaseModel, Field


class {{ model_name }}(BaseModel):
    """
    Pydantic model for {{ model_name }}.

    Used as the Event/State type for the {{ agent_name }}.
    """
    request_id: str = Field(..., description="Unique ID for the incoming request.")
    input_data: str = Field(..., description="Initial input data from the client.")
    
    # Add your custom fields here
    status: str = Field("Pending", description="Processing status.")
