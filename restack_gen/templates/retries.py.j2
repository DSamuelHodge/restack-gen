# @generated by restack_gen v{{ generator_version }} ({{ timestamp }})
# command: {{ command }}
"""Retry policies for {{ project_name }}."""

from datetime import timedelta
from restack_ai import RetryPolicy


DEFAULT_RETRY = RetryPolicy(
    initial_interval=timedelta(seconds=1),
    backoff_coefficient=2.0,
    maximum_interval=timedelta(seconds=60),
    maximum_attempts=5,
)
"""Default retry policy: exponential backoff from 1s to 60s, max 5 attempts."""


AGGRESSIVE_RETRY = RetryPolicy(
    initial_interval=timedelta(milliseconds=100),
    backoff_coefficient=2.0,
    maximum_interval=timedelta(seconds=10),
    maximum_attempts=10,
)
"""Aggressive retry policy: fast retries for transient failures."""


NO_RETRY = RetryPolicy(
    maximum_attempts=1,
)
"""No retry policy: fail immediately on error."""


LONG_RETRY = RetryPolicy(
    initial_interval=timedelta(seconds=5),
    backoff_coefficient=2.0,
    maximum_interval=timedelta(minutes=5),
    maximum_attempts=20,
)
"""Long retry policy: for expensive operations that may take time to recover."""
